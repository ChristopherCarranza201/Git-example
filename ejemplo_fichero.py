
# code <nombre_fichero.py> (crear fichero)
# code . (abrir fichero existente a visual studio)

# (Identificar a un usuario para reflejarlo como archivo ".git" oculto)
# git config --global user.email "you@example.com"
# git config --global user.name "Your Name"

# cd <tab_para_seleccionar> (localizar carpeta en disco C:)
# ls (invocar la librería de archivos de la carpeta consultada)

# git status (checar de la rama "main/master" si se ha realizado algún commit de los ficheros)
# git add <nombre_fichero.py> (salvar el fichero y poniendo en status "stage")
# git commit -m "comentario del fichero" (realizar un commit del fichero existente)
# git branch <nombre_rama_principal> (muestra el nombre de la ramificación del fichero "main")

# git log (abrir el commit realizado del fichero)
# git checkout <tab_para_seleccionar_fichero> (vuelve a la última versión guardada)
# git reset <tab_para_seleccionar_fichero> (indicar modo "unstaged" que el fichero no guardara los cambios que se harán)

# git log --graph --pretty -oneline (simplificar historial de commits) 
# git config --global alias.<name_of_function> (create a simple command to summon the git log graph...)

